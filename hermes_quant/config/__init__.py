"""hermes_quant.config — typed configuration models.

ADR-0038 §D.6 (P12) — TradingAgents pattern backfill, Wave D Track A.

Submodules:
    - vendor_config: two-level vendor selection (category default +
      per-method override) for the data-layer dispatch table.
"""
