# ADR-0091 Option-E acceptance-gate audit (2026-06-15)

Produced by the `mega-adr0091-accept` workflow (`wf_464f3587`) — a 12-item audit of the ADR-0091
Option-E acceptance gate against ACTUAL code, reconciling the ADR's drifted test paths to reality
and filling the genuinely-missing tests. Verdict: **gate_green=true** (10/12 test-provable items
MET/WROTE; 2 operator-gated). This is the evidence the ADR's ACCEPTED banner references.

---

# ADR-0091 Option E — Acceptance Gate Audit (REVIEWER VERDICT)

Worktree: /mnt/e/CS/github/hermes-quant-wt-adr0091  branch inc/adr0091  HEAD d88a369
Reviewer re-ran the canonical gate subset independently (flag-OFF AND flag-ON).

## Gate run results (reviewer, from inside the worktree)

- Canonical 12-file gate subset, flag-OFF: **137 passed** (8.52s) — byte-identical to today's behavior.
- Canonical 12-file gate subset, flag-ON (global force): **129 passed, 8 failed**.
  The 8 failures are ALL in tests/state/test_portfolio_state.py — PRE-EXISTING legacy
  delta-summing tests (last touched by commit 80b36e5 cs52+cs57, `git diff HEAD` EMPTY this
  session) that hand-feed DELTA-shaped records (e.g. fill 0.10 then -0.05 expecting sum 0.05)
  and do NOT opt into HERMES_QUANT_DELTA_NORMALIZER. They pass flag-OFF in isolation (8 passed).
  Under a globally-forced flag-ON the normalizer re-interprets their delta records as ABSOLUTE
  targets, so -0.05 means "target -0.05" -> net -0.05 not 0.05. This is an ARTIFACT of forcing
  the whole subset ON, NOT a gate regression: the flag is default-OFF and these legacy tests
  never enable it. The SAME file's own flag-ON re-affirmation test (line 316 pins the flag)
  PASSES flag-ON.
- Gate-RELEVANT files (the 12 new tests + normalizer-correcting tests, excluding the legacy
  delta-sum file), flag-ON: **84 passed**.

## RED-proof (keystone non-vacuity, reviewer-run)

Broke FillDeltaNormalizer.delta_from_net absolute-target branch to a raw pass-through
(`return absolute`) -> test_four_fold_cross_parity_flag_on FAILED `assert 12 == 1`
(12 phantom FIFO AAPL lots instead of the single collapsed lot, fill_delta_normalizer_shared.py:137).
Reverted to byte-identical (sha256 4d7e82c... restored, `git diff` on all production fold logic
EMPTY) and re-confirmed GREEN (4 passed). The keystone is load-bearing.

## 12-item disposition

| # | Gate item | Disposition |
|---|-----------|-------------|
| 1 | Contract fix: react/base.py docstring + nullable schema_version | MET (no edit needed) — react/base.py:8-22 (schema_version contract), :43-52 (fill_size_pct=ABSOLUTE target), :65, :83-89 (nullable+defaulted back-compat); SCHEMA_ABSOLUTE_TARGET imported from pdr_core.contracts:22; is_absolute_target_record :25-33 |
| 2 | Producers UNCHANGED (still-writes-absolute-target + stamps schema_version) | WROTE@tests/unit/test_producer_absolute_target_unchanged.py::test_paper_reactor_still_writes_absolute_target :81 + ::test_record_stamps_schema_version :112 |
| 3 | One shared normalizer 4-FOLD parity + ordering + incr-vs-rebuild + architectural | WROTE@tests/state/test_fill_delta_normalizer_shared.py::test_four_fold_cross_parity_flag_on :102 (a, parity all 4 folds), ::test_same_asof_tie_identical_delta_stream_both_consumers :205 (b, ordering), ::test_architectural_single_carry_forward_symbol :253 (d). RED-proven non-vacuous. |
| 4 | reaffirmation_does_not_inflate (AAPL 12x / BA 6x) | MET@tests/state/test_normalizer_wired_fold.py::test_flag_on_reaffirmation_does_not_inflate :60 + ::test_flag_on_cash_moves_once_for_reaffirmations :87; tests/state/test_portfolio_state.py:316 (file's own flag-ON reaffirm test); tests/unit/test_portfolio_state_accounting.py:402 |
| 5 | target_change_and_flip (5->7->7 = +5,+2,0; +5->-5 flip) | MET@tests/unit/test_fill_delta_normalizer.py::test_genuine_change_emits_increment :56 + ::test_flip_emits_signed_delta :63; reaffirmed in test_normalizer_wired_fold.py |
| 6 | det_equity_quantity_path (reactor_metadata.quantity carry-forward) | MET@tests/unit/test_fill_delta_normalizer.py::test_quantity_lane_carries_forward_in_shares :79; test_producer_absolute_target_unchanged.py::test_det_equity_quantity_lane_is_absolute_target :162 |
| 7 | legacy_records_interpreted_as_absolute_target (no schema_version) | MET@tests/unit/test_fill_delta_normalizer.py::test_absolute_target_schema_version_explicit_same_as_none :97; react/base.py:25-33 |
| 8 | Reconcile semantics: DERIVED net not raw fill_size_pct; OLD-vs-NEW nonzero | WROTE@tests/state/test_reconcile_derived_net_and_log_checksum.py::test_reconcile_reads_derived_net_not_raw_fill_size :110 + ::test_old_fold_vs_new_fold_reports_nonzero_divergence :132. ops/scripts/quant-ledger-reconcile.py:_positions :39-51 reads positions.quantity (derived), _diff :54-58 compares derived nets. |
| 9 | Conftest isolation: state.db / executions.jsonl / QUANT_HOME | MET@tests/conftest.py: _isolate_portfolio_state_db :78 (state.db) + _isolate_quant_home_and_execution_bus :111 (QUANT_HOME + executions.jsonl) + _isolate_hermes_quant_flags :193 (no flag leakage). All autouse. |
| 10 | Full sweep green; reconstruct seed unchanged; executions.jsonl BYTE-IDENTICAL | WROTE@tests/state/test_reconcile_derived_net_and_log_checksum.py::test_executions_log_byte_identical_across_flag_flip :188 (sha256 identical across flag-OFF rebuild + flag-ON rebuild + settlement pass) + ::test_record_count_unchanged_by_rebuild :218 |
| 11 | Operator-gated live heal (enable flag -> quant-ledger-reconcile --apply on real book) | N/A-operator-gated (live money mutation; human call) |
| 12 | Cross-family adversarial review (>=3 families) confirms structural enforcement, no new P0 | N/A-operator-gated (review process) |

## Cleanliness

- git diff HEAD --stat: EMPTY (no tracked file modified).
- Untracked: ONLY the 3 new test files. No production fold logic (fill_delta_normalizer.py /
  portfolio_state.py / settlement_loop.py / portfolio/state.py), no react/base.py edit, no ADR,
  no seeds, no cowork-quant/, no pdr_core/, no .env/cron.
- ruff check on the 3 new files: All checks passed.

## VERDICT

GATE GREEN. All 10 test-provable Option-E gate items MET or WROTE by real passing tests
(flag-OFF byte-identical at 137 green; flag-ON corrects inflation, 84 gate-relevant green +
the legacy file's own flag-ON reaffirm test green). The 4-fold parity keystone is RED-proven
non-vacuous. Items 11 and 12 are operator-gated (live heal + cross-family review). The 8
flag-ON failures in test_portfolio_state.py are pre-existing legacy delta-semantics tests that
do not opt into the default-OFF flag — not a gate regression. Orchestrator may flip ADR-0091
to accepted with the reconciled checklist above and close ra01/ra03.
